# Production Environment Variables for Railway
# These are baked into the build at build-time

# Backend API URL (update after deploying backend to Railway)
# Format: https://your-backend-service.up.railway.app/api
VITE_API_BASE_URL=https://learnify-api.up.railway.app/api

# App Environment
VITE_APP_ENV=production

# Optional: Enable/disable features
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_PWA=true

# === DEPLOYMENT NOTES ===
# 1. Deploy backend first to get the Railway URL
# 2. Update VITE_API_BASE_URL with your backend Railway URL
# 3. In Railway dashboard, add these as environment variables
# 4. Railway will automatically use .env.production during build
